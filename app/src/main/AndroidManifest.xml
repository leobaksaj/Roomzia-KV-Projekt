<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.roomzia">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Roomzia"
        android:usesCleartextTraffic="true">
        <activity android:name=".ActivityDoneJobCleaner"></activity>
        <activity android:name=".ActivityMyOffer" />
        <activity android:name=".OldAdsActivity" />
        <activity android:name=".OfferReviewActivity" />
        <activity android:name=".CreateOfferCleanerActivity" />
        <activity android:name=".CreateAdActivity" />
        <activity android:name=".CleanerActivity" />
        <activity android:name=".ProviderAdsActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>